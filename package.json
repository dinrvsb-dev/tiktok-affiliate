{
  "name": "tiktok-live-reporting",
  "version": "1.0.0",
  "description": "WhatsApp to Google Sheets reporting workflow for TikTok live screenshot submissions.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.90.0",
    "express": "^4.19.2",
    "googleapis": "^140.0.1",
    "openai": "^6.37.0"
  }
}
